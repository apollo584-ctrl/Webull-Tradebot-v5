# GitHub Setup Checkpoint

The project uses a generic GitHub CLI workflow.

The current project is always determined by the active folder, not by this document. The first setup binds the current Git root, origin remote, and GitHub default branch. Later helper commands verify that binding before branch, commit, push, or PR actions.

No repository, branch, or push target is inferred from the setup guide. No application-specific, trading, evaluation, or model instructions belong here.